{{- if and .Values.kagentiOperator .Values.kagentiOperator.spiffeAuth .Values.kagentiOperator.spiffeAuth.enabled }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kagenti-operator-client-bootstrap
  namespace: {{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}
  labels:
    app.kubernetes.io/name: kagenti-operator-client-bootstrap
    app.kubernetes.io/component: bootstrap
    app.kubernetes.io/part-of: kagenti
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kagenti-operator-client-bootstrap
  namespace: {{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["keycloak-initial-admin"]
  verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kagenti-operator-client-bootstrap
  namespace: {{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kagenti-operator-client-bootstrap
subjects:
- kind: ServiceAccount
  name: kagenti-operator-client-bootstrap
  namespace: {{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kagenti-operator-client-bootstrap-keycloak
  namespace: keycloak
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["keycloak-initial-admin"]
  verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kagenti-operator-client-bootstrap
  namespace: keycloak
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
    helm.sh/hook-delete-policy: before-hook-creation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kagenti-operator-client-bootstrap-keycloak
subjects:
- kind: ServiceAccount
  name: kagenti-operator-client-bootstrap
  namespace: {{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: kagenti-operator-client-bootstrap
  namespace: {{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}
  labels:
    app.kubernetes.io/name: kagenti-operator-client-bootstrap
    app.kubernetes.io/component: bootstrap
    app.kubernetes.io/part-of: kagenti
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "10"
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kagenti-operator-client-bootstrap
        app.kubernetes.io/component: bootstrap
    spec:
      serviceAccountName: kagenti-operator-client-bootstrap
      restartPolicy: OnFailure
      containers:
      - name: bootstrap
        image: {{ .Values.kagentiOperator.spiffeAuth.bootstrapImage | default "ghcr.io/kagenti/kagenti/operator-spiffe-bootstrap:latest" }}
        imagePullPolicy: IfNotPresent
        env:
        - name: KEYCLOAK_URL
          value: "http://keycloak-service.keycloak.svc:8080"
        - name: KEYCLOAK_REALM
          value: "{{ .Values.keycloak.realm | default "kagenti" }}"
        - name: KEYCLOAK_ADMIN_SECRET_NAME
          value: "keycloak-initial-admin"
        - name: KEYCLOAK_ADMIN_SECRET_NAMESPACE
          value: "keycloak"
        - name: KEYCLOAK_ADMIN_USERNAME_KEY
          value: "username"
        - name: KEYCLOAK_ADMIN_PASSWORD_KEY
          value: "password"
        - name: SPIFFE_IDP_ALIAS
          value: "{{ .Values.spiffe.idpAlias | default "spire-spiffe" }}"
        - name: SPIRE_OIDC_URL
          value: "{{ .Values.spiffe.oidcDiscoveryURL | default "http://spire-spiffe-oidc-discovery-provider.zero-trust-workload-identity-manager.svc:8080" }}"
        - name: OPERATOR_CLIENT_ID
          value: {{ .Values.kagentiOperator.spiffeAuth.operatorClientID | default (printf "spiffe://%s/ns/%s/sa/%s" (.Values.spireTrustDomain | default "localtest.me") (.Values.kagentiOperator.namespace | default "kagenti-operator-system") (.Values.kagentiOperator.serviceAccountName | default "controller-manager")) | quote }}
        - name: OPERATOR_NAMESPACE
          value: "{{ .Values.kagentiOperator.namespace | default "kagenti-operator-system" }}"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1001
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
{{- end }}
